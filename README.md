This is a Milan Kotarlic bio and work website

Written in react

Tasks: 
 - Initialize react application (react init)
 
 - Use react router for different pages, should have basic pages home, about, contact, gallery (see attached figma design for example but adjust). Directories like any app, there should be a src directory, inside you can create components directory for reusable components like buttons, slides. Pages directory for each page like home, about, contact, gallery etc. Store directory for the store (defaultState, reducer, index and actions). 
 
 - Create react store for storing the data on the frontend, and then create one json file with basic site data (like homepage.title, homepage.description, about.title) and then fetch all the data from json file and store in the state.
 
 - Learn how to inspect figma elements, and do a mobile first design based on this example: 
 https://www.figma.com/design/aXNNablESkMjfDqzGn5IKG/Digital-Agency-Company-Website-UI-Design-Template-in-Dark-Theme---FREE-Editable----Community-?node-id=3-53&p=f&t=AF07kBs5qyWfjPl9-0
 Use sass for styles, create variables file to define colors used, font sizes, base font, use BEM methodology to name css classes, and adjusted 7-1 sass architecture (based on specific needs).

 - One of the pages should be the gallery, research how to display youtube playlist videos in a dynamic way, new video should automatically appear on the website (check this chatGPT prompt https://chatgpt.com/share/68dc00d9-10a4-800c-a731-4d347590df28)

 - All text in the app should be just reference from the "fake backend" json file, and then we should have multiple json files that represent language (create two languages srb.json and en.json). Create language provider 